## Packages
recharts | Dashboard charts for revenue vs costs
framer-motion | Smooth transitions and animations for the UI
date-fns | Date formatting for events and tables

## Notes
- Using Shadcn UI components which are already present in the codebase
- Authentication is handled via session cookies (credentials: "include")
- Financial calculations are backend-driven; UI just displays the results
- Sidebar navigation required for: Dashboard, Events, Catalog, Employees, Fleet
